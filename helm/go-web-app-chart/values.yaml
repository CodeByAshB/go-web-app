replicaCount: 2

image:
  repository: public.ecr.aws/o9g6t1r4/go-web-app
  tag: ""              # ❗ EMPTY by default
  pullPolicy: Always

service:
  type: NodePort
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: nginx

